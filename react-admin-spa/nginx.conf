server {
    listen       5000;
    listen       [::]:5000;
    server_name  _;

    # Root folder with built SPA
    root   /usr/share/nginx/html;
    index  index.html;

    # Security + allow embedding in Telegram
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;
    # Allow this app to be embedded by Telegram, and allow frames to Telegram domains
    add_header Content-Security-Policy "default-src 'self' data: blob: https:; script-src 'self' 'unsafe-inline' 'unsafe-eval' https: https://telegram.org https://t.me; style-src 'self' 'unsafe-inline' https:; img-src * data: blob:; font-src 'self' data: https:; connect-src *; frame-src 'self' https://oauth.telegram.org https://t.me https://web.telegram.org; frame-ancestors 'self' https://web.telegram.org https://*.telegram.org https://t.me;" always;

    # Serve static files with basic caching
    location ~* \.(?:js|css|png|jpg|jpeg|gif|svg|ico|woff2?|ttf|eot)$ {
        try_files $uri =404;
        access_log off;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    # SPA fallback to index.html
    location / {
        try_files $uri /index.html;
    }

    # Optional: UI-specific healthcheck (HTTP 200, no body)
    location = /ui-healthz {
        return 200;
    }
}
